<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}} - StartEducation</title>
    
    <!-- Favicon -->
    <link rel="icon" type="image/svg+xml" href="/images/favicon.svg">
    
    <!-- Bootstrap 5.3 CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Bootstrap Icons -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css" rel="stylesheet">
    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    
    <style>
        :root {
            --primary-color: #ea580c;
            --primary-hover: #c2410c;
            --success-color: #10b981;
            --warning-color: #f59e0b;
            --danger-color: #ef4444;
            --text-dark: #1f2937;
            --text-muted: #6b7280;
            --border-color: #e5e7eb;
            --bg-light: #f8fafc;
        }

        * {
            font-family: 'Inter', sans-serif;
        }

        body {
            background: linear-gradient(135deg, #f8fafc 0%, #e2e8f0 100%);
            color: var(--text-dark);
            min-height: 100vh;
        }

        .main-container {
            max-width: 1100px;
            margin: 0 auto;
            padding: 1.5rem;
        }

        .header-section {
            background: white;
            border-radius: 12px;
            box-shadow: 0 3px 15px rgba(0, 0, 0, 0.08);
            padding: 1.5rem;
            margin-bottom: 1.5rem;
            border-left: 4px solid var(--primary-color);
        }

        .header-title {
            display: flex;
            align-items: center;
            justify-content: space-between;
            flex-wrap: wrap;
            gap: 1rem;
        }

        .header-title h1 {
            color: var(--text-dark);
            font-weight: 700;
            margin: 0;
            display: flex;
            align-items: center;
            gap: 0.6rem;
        }

        .profile-avatar {
            width: 64px;
            height: 64px;
            border-radius: 50%;
            background: linear-gradient(135deg, var(--primary-color) 0%, var(--primary-hover) 100%);
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 1.6rem;
            color: white;
            font-weight: 600;
        }

        .profile-info {
            flex: 1;
            margin-left: 0.8rem;
        }

        .profile-info h1 {
            font-size: 1.4rem;
            margin-bottom: 0.4rem;
        }

        .profile-meta {
            color: var(--text-muted);
            font-size: 0.8rem;
            margin-bottom: 0.4rem;
        }

        .role-badge {
            display: inline-flex;
            align-items: center;
            gap: 0.4rem;
            padding: 0.4rem 0.8rem;
            background: var(--bg-light);
            color: var(--primary-color);
            border-radius: 16px;
            font-size: 0.75rem;
            font-weight: 600;
            border: 2px solid var(--primary-color);
        }

        .stats-row {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(160px, 1fr));
            gap: 1.2rem;
            margin-top: 1.2rem;
        }

        .stat-card {
            background: var(--bg-light);
            border-radius: 9px;
            padding: 1.2rem;
            text-align: center;
            border: 1px solid var(--border-color);
            transition: transform 0.2s ease;
        }

        .stat-card:hover {
            transform: translateY(-2px);
        }

        .stat-icon {
            width: 40px;
            height: 40px;
            border-radius: 50%;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            font-size: 1.2rem;
            margin-bottom: 0.8rem;
            background: linear-gradient(135deg, var(--primary-color) 0%, var(--primary-hover) 100%);
            color: white;
        }

        .stat-number {
            font-size: 1.6rem;
            font-weight: 700;
            color: var(--primary-color);
            display: block;
        }

        .stat-label {
            color: var(--text-muted);
            font-size: 0.75rem;
            margin-top: 0.4rem;
        }

        .content-grid {
            display: grid;
            grid-template-columns: 1fr 320px;
            gap: 1.5rem;
            margin-top: 1.5rem;
        }

        @media (max-width: 1024px) {
            .content-grid {
                grid-template-columns: 1fr;
            }
        }

        /* Make the profile settings card span the full width of the container (same as header) */
        .profile-settings {
            grid-column: 1 / -1;
        }

        /* Place recent activity in the right column when available */
        .recent-activity {
            grid-column: 2 / 3;
        }

        .main-card {
            background: white;
            border-radius: 12px;
            box-shadow: 0 3px 15px rgba(0, 0, 0, 0.08);
            overflow: hidden;
        }

        .card-header {
            background: var(--bg-light);
            padding: 1.2rem;
            border-bottom: 1px solid var(--border-color);
        }

        .card-header h3 {
            font-size: 1rem;
            font-weight: 600;
            color: var(--text-dark);
            margin: 0;
            display: flex;
            align-items: center;
            gap: 0.6rem;
        }

        .card-body {
            padding: 1.2rem;
        }

        .form-section {
            margin-bottom: 1.5rem;
        }

        .form-section:last-child {
            margin-bottom: 0;
        }

        .section-title {
            font-size: 0.95rem;
            font-weight: 600;
            color: var(--text-dark);
            margin-bottom: 0.8rem;
            display: flex;
            align-items: center;
            gap: 0.4rem;
            padding-bottom: 0.4rem;
            border-bottom: 2px solid var(--border-color);
        }

        .form-label {
            font-weight: 600;
            color: var(--text-dark);
            margin-bottom: 0.4rem;
            font-size: 0.85rem;
        }

        .form-control {
            border: 2px solid var(--border-color);
            border-radius: 6px;
            padding: 0.6rem 0.8rem;
            font-size: 0.85rem;
            transition: border-color 0.3s ease;
        }

        .form-control:focus {
            border-color: var(--primary-color);
            box-shadow: 0 0 0 0.15rem rgba(234, 88, 12, 0.25);
        }

        .btn-primary {
            background: linear-gradient(135deg, var(--primary-color) 0%, var(--primary-hover) 100%);
            border: none;
            border-radius: 6px;
            font-weight: 600;
            padding: 0.6rem 1.2rem;
            font-size: 0.85rem;
            transition: all 0.3s ease;
        }

        .btn-primary:hover {
            background: linear-gradient(135deg, var(--primary-hover) 0%, #9a3412 100%);
            transform: translateY(-1px);
            box-shadow: 0 6px 15px rgba(234, 88, 12, 0.3);
        }

        .btn-success {
            background: var(--success-color);
            border: none;
            border-radius: 6px;
            font-weight: 600;
            padding: 0.6rem 1.2rem;
            font-size: 0.85rem;
            transition: all 0.3s ease;
        }

        .btn-success:hover {
            background: #059669;
            transform: translateY(-1px);
        }

        .instructor-actions {
            padding: 1rem 0;
        }

        .btn-outline-primary {
            border: 2px solid var(--primary-color);
            color: var(--primary-color);
            background: transparent;
            border-radius: 8px;
            font-weight: 600;
            padding: 0.7rem 1.5rem;
            font-size: 0.9rem;
            transition: all 0.3s ease;
            text-decoration: none;
            display: inline-flex;
            align-items: center;
        }

        .btn-outline-primary:hover {
            background: var(--primary-color);
            color: white;
            transform: translateY(-2px);
            box-shadow: 0 4px 12px rgba(234, 88, 12, 0.25);
        }

        .btn-outline-success {
            border: 2px solid var(--success-color);
            color: var(--success-color);
            background: transparent;
            border-radius: 8px;
            font-weight: 600;
            padding: 0.7rem 1.5rem;
            font-size: 0.9rem;
            transition: all 0.3s ease;
            text-decoration: none;
            display: inline-flex;
            align-items: center;
        }

        .btn-outline-success:hover {
            background: var(--success-color);
            color: white;
            transform: translateY(-2px);
            box-shadow: 0 4px 12px rgba(16, 185, 129, 0.25);
        }

        .student-actions {
            padding: 1rem 0;
        }

        .subscription-card {
            position: relative;
        }

        .subscription-card.active {
            background: linear-gradient(135deg, #10b981 0%, #059669 100%);
            color: white;
        }

        .subscription-card.active .stat-icon {
            background: rgba(255, 255, 255, 0.2);
            color: white;
        }

        .subscription-card.inactive {
            background: linear-gradient(135deg, #ef4444 0%, #dc2626 100%);
            color: white;
        }

        .subscription-card.inactive .stat-icon {
            background: rgba(255, 255, 255, 0.2);
            color: white;
        }

        .stat-detail {
            font-size: 0.75rem;
            margin-top: 0.25rem;
            opacity: 0.8;
        }

        /* Make the subscription card clickable when wrapped in a link */
        .subscription-link {
            display: inline-block;
            text-decoration: none;
            color: inherit;
        }

        .subscription-link .stat-card {
            margin: 0;
        }

        .activity-item {
            display: flex;
            align-items: center;
            gap: 0.8rem;
            padding: 0.8rem;
            border-bottom: 1px solid #f1f5f9;
            transition: background-color 0.2s ease;
        }

        .activity-item:last-child {
            border-bottom: none;
        }

        .activity-item:hover {
            background-color: rgba(234, 88, 12, 0.05);
        }

        .activity-icon {
            width: 32px;
            height: 32px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 0.85rem;
            flex-shrink: 0;
            background: linear-gradient(135deg, var(--primary-color) 0%, var(--primary-hover) 100%);
            color: white;
        }

        .activity-content {
            flex: 1;
        }

        .activity-title {
            font-weight: 600;
            color: var(--text-dark);
            margin-bottom: 0.2rem;
            font-size: 0.9rem;
        }

        .activity-time {
            font-size: 0.75rem;
            color: var(--text-muted);
        }

        .activity-value {
            font-weight: 600;
            color: var(--primary-color);
            font-size: 0.85rem;
        }

        .back-btn {
            position: fixed;
            top: 1.5rem;
            left: 1.5rem;
            background: white;
            border: 2px solid var(--border-color);
            width: 38px;
            height: 38px;
            border-radius: 6px;
            display: flex;
            align-items: center;
            justify-content: center;
            color: var(--text-dark);
            text-decoration: none;
            transition: all 0.2s ease;
            box-shadow: 0 3px 15px rgba(0, 0, 0, 0.08);
            z-index: 1000;
            font-size: 0.9rem;
        }

        .back-btn:hover {
            background: var(--primary-color);
            color: white;
            border-color: var(--primary-color);
            transform: translateY(-1px);
        }

        .alert {
            border-radius: 6px;
            border: none;
            padding: 0.8rem;
            margin-bottom: 0.8rem;
            font-size: 0.85rem;
        }

        .alert-success {
            background: #d1fae5;
            color: #065f46;
            border-left: 3px solid var(--success-color);
        }

        .alert-danger {
            background: #fee2e2;
            color: #991b1b;
            border-left: 3px solid var(--danger-color);
        }

        .btn-loading .spinner-border {
            width: 1rem;
            height: 1rem;
        }

        @media (max-width: 768px) {
            .main-container {
                padding: 0.8rem;
            }

            .back-btn {
                top: 0.8rem;
                left: 0.8rem;
                width: 32px;
                height: 32px;
                font-size: 0.8rem;
            }

            .header-section {
                padding: 1.2rem;
            }

            .header-title {
                flex-direction: column;
                text-align: center;
            }

            .profile-info {
                margin-left: 0;
                margin-top: 0.8rem;
            }

            .profile-info h1 {
                font-size: 1.2rem;
            }

            .stats-row {
                grid-template-columns: repeat(2, 1fr);
                gap: 0.8rem;
            }

            .content-grid {
                grid-template-columns: 1fr;
                gap: 0.8rem;
            }

            /* On small screens let elements flow naturally */
            .profile-settings {
                grid-column: auto;
            }

            .recent-activity {
                grid-column: auto;
            }

            .card-header,
            .card-body {
                padding: 0.8rem;
            }

            .stat-card {
                padding: 1rem;
            }

            .activity-item {
                padding: 0.6rem;
            }
        }
  </style>
</head>
<body>
    <!-- Back Button -->
    <a href="/dashboard" class="back-btn" title="Volver al Dashboard">
        <i class="bi bi-arrow-left"></i>
    </a>

    <div class="main-container">
        <!-- Header Section -->
        <div class="header-section">
            <div class="header-title">
                <div class="d-flex align-items-center">
                    <div class="profile-avatar">
                        {{initials user.nombre user.apellido}}
                    </div>
                    <div class="profile-info">
                        <h1>{{user.nombre}} {{user.apellido}}</h1>
                        <div class="profile-meta">@{{user.nombre_usuario}} • {{user.email}}</div>
                        <div class="role-badge">
                            <i class="bi bi-{{#if isInstructor}}mortarboard{{else}}person{{/if}}"></i>
                            {{#if isInstructor}}Instructor{{else}}Estudiante{{/if}}
                        </div>
                    </div>
                </div>
                <div class="text-muted">
                    <small><i class="bi bi-calendar-check"></i> Miembro desde {{user.fecha_registro_formateada}}</small>
                </div>
            </div>

            <!-- Statistics -->
            <div class="stats-row">
                {{#if isInstructor}}
                    <div class="stat-card">
                        <div class="stat-icon">
                            <i class="bi bi-book"></i>
                        </div>
                        <span class="stat-number">{{stats.cursos_creados}}</span>
                        <div class="stat-label">Cursos Creados</div>
                    </div>
                    
                    <div class="stat-card">
                        <div class="stat-icon">
                            <i class="bi bi-cart-check"></i>
                        </div>
                        <span class="stat-number">{{stats.ventas_totales}}</span>
                        <div class="stat-label">Ventas Totales</div>
                    </div>
                    
                    <div class="stat-card">
                        <div class="stat-icon">
                            <i class="bi bi-currency-dollar"></i>
                        </div>
                        <span class="stat-number">${{stats.ingresos_totales}}</span>
                        <div class="stat-label">Ingresos Totales</div>
                    </div>
                    
                    <div class="stat-card">
                        <div class="stat-icon">
                            <i class="bi bi-people"></i>
                        </div>
                        <span class="stat-number">{{stats.estudiantes_totales}}</span>
                        <div class="stat-label">Estudiantes</div>
                    </div>
                    
                    <div class="stat-card">
                        <div class="stat-icon">
                            <i class="bi bi-star"></i>
                        </div>
                        <span class="stat-number">{{stats.calificacion_promedio}}</span>
                        <div class="stat-label">Calificación Promedio</div>
                    </div>
                {{else}}
                    <div class="stat-card">
                        <div class="stat-icon">
                            <i class="bi bi-book"></i>
                        </div>
                        <span class="stat-number">{{stats.cursos_comprados}}</span>
                        <div class="stat-label">Cursos Comprados</div>
                    </div>
                    
                    <div class="stat-card">
                        <div class="stat-icon">
                            <i class="bi bi-play-circle"></i>
                        </div>
                        <span class="stat-number">{{stats.videos_completados}}</span>
                        <div class="stat-label">Videos Completados</div>
                    </div>
                    
                    <div class="stat-card">
                        <div class="stat-icon">
                            <i class="bi bi-award"></i>
                        </div>
                        <span class="stat-number">{{stats.certificados_obtenidos}}</span>
                        <div class="stat-label">Certificados</div>
                    </div>
                    
                    <div class="stat-card">
                        <div class="stat-icon">
                            <i class="bi bi-heart"></i>
                        </div>
                        <span class="stat-number">{{stats.cursos_favoritos}}</span>
                        <div class="stat-label">Favoritos</div>
                    </div>
                    

                    <!-- Subscription Status Card -->
                    {{#if stats.subscription.tiene_suscripcion_activa}}
                    <a href="/suscripciones" class="subscription-link">
                      <div class="stat-card subscription-card active">
                          <div class="stat-icon">
                              <i class="bi bi-shield-check"></i>
                          </div>
                          <span class="stat-number">Activa</span>
                          <div class="stat-label">{{stats.subscription.nombre_membresia}}</div>
                          <div class="stat-detail">{{stats.subscription.dias_restantes}} días restantes</div>
                      </div>
                    </a>
                    {{else}}
                      <div class="stat-card subscription-card {{#if stats.subscription}}inactive{{/if}}">
                          <div class="stat-icon">
                              <i class="bi bi-shield-x"></i>
                          </div>
                          {{#if stats.subscription}}
                              <span class="stat-number">{{stats.subscription.estatus}}</span>
                              <div class="stat-label">{{stats.subscription.nombre_membresia}}</div>
                              <div class="stat-detail">Expirada</div>
                          {{else}}
                              <span class="stat-number">Sin Plan</span>
                              <div class="stat-label">Suscripción</div>
                              <div class="stat-detail">No tienes suscripción</div>
                          {{/if}}
                      </div>
                    {{/if}}
                {{/if}}
            </div>
            
            <!-- Instructor Actions Section -->
            {{#if isInstructor}}
            <div class="instructor-actions mt-4">
                <div class="d-flex justify-content-center">
                    <a href="/two-factor/manage" class="btn btn-outline-primary">
                        <i class="bi bi-shield-lock me-2"></i>
                        Recuperación de cuenta
                    </a>
                </div>
            </div>
            {{/if}}

            <!-- Student Subscription Actions -->
            {{#if isStudent}}
            <div class="student-actions mt-4">
                <div class="d-flex justify-content-center">
                    {{#unless stats.subscription.tiene_suscripcion_activa}}
                        <a href="/membresias" class="btn btn-primary">
                            <i class="bi bi-plus-circle me-2"></i>
                            Obtener Suscripción
                        </a>
                    {{/unless}}
                </div>
            </div>
            {{/if}}
        </div>

        <!-- Main Content -->
        <div class="content-grid">
            <!-- Profile Settings -->
            <div class="main-card profile-settings">
                <div class="card-header">
                    <h3><i class="bi bi-gear"></i>Configuración del Perfil</h3>
                </div>
                <div class="card-body">
                    <!-- Alerts -->
                    <div id="profile-alerts"></div>
                    
                    <!-- Personal Information -->
                    <div class="form-section">
                        <div class="section-title">
                            <i class="bi bi-person"></i>
                            Información Personal
                        </div>
                        <form id="profile-form">
                            <div class="row">
                                <div class="col-md-6 mb-3">
                                    <label class="form-label">Nombre</label>
                                    <input type="text" class="form-control" name="nombre" value="{{user.nombre}}" required>
                                </div>
                                <div class="col-md-6 mb-3">
                                    <label class="form-label">Apellido</label>
                                    <input type="text" class="form-control" name="apellido" value="{{user.apellido}}" required>
                                </div>
                            </div>
                            <div class="row">
                                <div class="col-md-6 mb-3">
                                    <label class="form-label">Nombre de Usuario</label>
                                    <input type="text" class="form-control" name="nombre_usuario" value="{{user.nombre_usuario}}" required>
                                </div>
                                <div class="col-md-6 mb-3">
                                    <label class="form-label">Email</label>
                                    <input type="email" class="form-control" name="email" value="{{user.email}}" required>
                                </div>
                            </div>
                            <button type="submit" class="btn btn-primary">
                                <span class="btn-text">Actualizar Información</span>
                                <span class="spinner-border spinner-border-sm me-2 d-none" role="status"></span>
                            </button>
                        </form>
                    </div>

                    <hr>

                    <!-- Change Password -->
                    <div class="form-section">
                        <div class="section-title">
                            <i class="bi bi-shield-lock"></i>
                            Cambiar Contraseña
                        </div>
                        <form id="password-form">
                            <div class="mb-3">
                                <label class="form-label">Contraseña Actual</label>
                                <input type="password" class="form-control" name="currentPassword" required>
                            </div>
                            <div class="row">
                                <div class="col-md-6 mb-3">
                                    <label class="form-label">Nueva Contraseña</label>
                                    <input type="password" class="form-control" name="newPassword" required>
                                </div>
                                <div class="col-md-6 mb-3">
                                    <label class="form-label">Confirmar Nueva Contraseña</label>
                                    <input type="password" class="form-control" name="confirmPassword" required>
                                </div>
                            </div>
                            <button type="submit" class="btn btn-success">
                                <span class="btn-text">Cambiar Contraseña</span>
                                <span class="spinner-border spinner-border-sm me-2 d-none" role="status"></span>
                            </button>
                        </form>
                    </div>
                </div>
            </div>

            <!-- Recent Activity - Solo para instructores -->
            {{#if isInstructor}}
            <div class="main-card recent-activity">
                <div class="card-header">
                    <h3><i class="bi bi-activity"></i>Actividad Reciente</h3>
                </div>
                <div class="card-body p-0">
                    {{#if recentActivity.length}}
                        {{#each recentActivity}}
                            <div class="activity-item">
                                <div class="activity-icon">
                                    <i class="bi bi-{{#if (eq tipo 'Compra')}}cart-check{{else}}play-circle{{/if}}"></i>
                                </div>
                                <div class="activity-content">
                                    <div class="activity-title">{{descripcion}}</div>
                                    <div class="activity-time">{{tiempo_transcurrido}} • {{fecha_formateada}}</div>
                                </div>
                                {{#if valor}}
                                    <div class="activity-value">${{valor}}</div>
                                {{/if}}
                            </div>
                        {{/each}}
                    {{else}}
                        <div class="text-center p-4">
                            <i class="bi bi-clock-history text-muted" style="font-size: 3rem;"></i>
                            <p class="text-muted mt-2">No hay actividad reciente</p>
                        </div>
                    {{/if}}
                </div>
            </div>
            {{/if}}
        </div>
    </div>

  <!-- Scripts -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
  
  <script>
    // Profile Form Handler
    document.getElementById('profile-form').addEventListener('submit', async function(e) {
      e.preventDefault();
      
      const form = this;
      const submitBtn = form.querySelector('button[type="submit"]');
      const btnText = submitBtn.querySelector('.btn-text');
      const spinner = submitBtn.querySelector('.spinner-border');
      
      // Show loading state
      submitBtn.disabled = true;
      btnText.textContent = 'Actualizando...';
      spinner.classList.remove('d-none');
      
      const formData = new FormData(form);
      const data = Object.fromEntries(formData);
      
      try {
        const response = await fetch('/perfil/actualizar', {
          method: 'POST',
          headers: {
            'Content-Type': 'application/json',
          },
          body: JSON.stringify(data)
        });
        
        const result = await response.json();
        
        if (result.success) {
          showAlert('success', result.message);
        } else {
          showAlert('danger', result.error);
        }
      } catch (error) {
        showAlert('danger', 'Error al actualizar perfil');
        console.error('Error:', error);
      } finally {
        // Hide loading state
        submitBtn.disabled = false;
        btnText.textContent = 'Actualizar Información';
        spinner.classList.add('d-none');
      }
    });

    // Password Form Handler
    document.getElementById('password-form').addEventListener('submit', async function(e) {
      e.preventDefault();
      
      const form = this;
      const submitBtn = form.querySelector('button[type="submit"]');
      const btnText = submitBtn.querySelector('.btn-text');
      const spinner = submitBtn.querySelector('.spinner-border');
      
      // Show loading state
      submitBtn.disabled = true;
      btnText.textContent = 'Cambiando...';
      spinner.classList.remove('d-none');
      
      const formData = new FormData(form);
      const data = Object.fromEntries(formData);
      
      try {
        const response = await fetch('/perfil/cambiar-password', {
          method: 'POST',
          headers: {
            'Content-Type': 'application/json',
          },
          body: JSON.stringify(data)
        });
        
        const result = await response.json();
        
        if (result.success) {
          showAlert('success', result.message);
          form.reset();
        } else {
          showAlert('danger', result.error);
        }
      } catch (error) {
        showAlert('danger', 'Error al cambiar contraseña');
        console.error('Error:', error);
      } finally {
        // Hide loading state
        submitBtn.disabled = false;
        btnText.textContent = 'Cambiar Contraseña';
        spinner.classList.add('d-none');
      }
    });

    // Show Alert Function
    function showAlert(type, message) {
      const alertsContainer = document.getElementById('profile-alerts');
      const alert = document.createElement('div');
      alert.className = `alert alert-${type} alert-dismissible fade show`;
      alert.innerHTML = `
        ${message}
        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
      `;
      
      alertsContainer.innerHTML = '';
      alertsContainer.appendChild(alert);
      
      // Auto dismiss after 5 seconds
      setTimeout(() => {
        if (alert.parentNode) {
          alert.classList.remove('show');
          setTimeout(() => {
            if (alert.parentNode) {
              alert.remove();
            }
          }, 150);
        }
      }, 5000);
    }
  </script>
</body>
</html>