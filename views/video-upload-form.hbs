<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}} - StartEducation</title>
    
    <!-- Bootstrap 5.3 CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Bootstrap Icons -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css" rel="stylesheet">
    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    
    <style>
        :root {
            --primary-color: #ea580c;
            --primary-hover: #c2410c;
            --success-color: #10b981;
            --warning-color: #f59e0b;
            --danger-color: #ef4444;
            --text-dark: #1f2937;
            --text-muted: #6b7280;
            --border-color: #e5e7eb;
            --bg-light: #f8fafc;
        }

        * {
            font-family: 'Inter', sans-serif;
        }

        body {
            background: linear-gradient(135deg, #f8fafc 0%, #e2e8f0 100%);
            color: var(--text-dark);
            min-height: 100vh;
        }

        .main-container {
            max-width: 1400px;
            margin: 0 auto;
            padding: 2rem;
        }

        .header-section {
            background: white;
            border-radius: 16px;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
            padding: 2rem;
            margin-bottom: 2rem;
            border-left: 5px solid var(--primary-color);
        }

        .header-title h1 {
            color: var(--text-dark);
            font-weight: 700;
            margin: 0;
            display: flex;
            align-items: center;
            gap: 0.75rem;
        }

        .header-subtitle {
            color: var(--text-muted);
            margin-top: 0.5rem;
            font-size: 1.1rem;
        }

        .upload-container {
            background: white;
            border-radius: 16px;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
            padding: 2rem;
        }

        .form-section {
            margin-bottom: 2rem;
            padding-bottom: 2rem;
            border-bottom: 1px solid var(--border-color);
        }

        .form-section:last-child {
            border-bottom: none;
            margin-bottom: 0;
            padding-bottom: 0;
        }

        .section-title {
            color: var(--text-dark);
            font-weight: 600;
            margin-bottom: 1.5rem;
            display: flex;
            align-items: center;
            gap: 0.75rem;
        }

        .file-drop-area {
            position: relative;
            border: 2px dashed var(--border-color);
            border-radius: 16px;
            padding: 3rem 2rem;
            text-align: center;
            cursor: pointer;
            transition: all 0.3s ease;
            background: linear-gradient(135deg, #fafbfc 0%, #f1f5f9 100%);
            min-height: 200px;
            display: flex;
            align-items: center;
            justify-content: center;
            flex-direction: column;
        }

        .file-drop-area:hover,
        .file-drop-area.dragover {
            border-color: var(--primary-color);
            background: rgba(234, 88, 12, 0.02);
            transform: translateY(-2px);
            box-shadow: 0 8px 25px rgba(234, 88, 12, 0.15);
        }

        .file-drop-area.has-file {
            border-color: var(--success-color);
            background: rgba(16, 185, 129, 0.02);
            box-shadow: 0 8px 25px rgba(16, 185, 129, 0.15);
        }

        .file-input {
            display: none;
        }

        .upload-progress {
            display: none;
            margin-top: 1rem;
        }

        .progress {
            height: 1rem;
            border-radius: 0.5rem;
            background: var(--bg-light);
        }

        .progress-bar {
            background: linear-gradient(90deg, var(--primary-color), var(--primary-hover));
            border-radius: 0.5rem;
        }

        .btn-primary {
            background: linear-gradient(135deg, var(--primary-color) 0%, var(--primary-hover) 100%);
            border: none;
            color: white;
            font-weight: 600;
            padding: 0.75rem 1.5rem;
            border-radius: 12px;
            transition: all 0.3s ease;
            text-transform: none;
            letter-spacing: 0.025em;
        }

        .btn-primary:hover {
            background: linear-gradient(135deg, var(--primary-hover) 0%, #9a3412 100%);
            transform: translateY(-2px);
            box-shadow: 0 8px 25px rgba(234, 88, 12, 0.25);
        }

        .btn-secondary {
            background: linear-gradient(135deg, #6b7280 0%, #4b5563 100%);
            border: none;
            color: white;
            font-weight: 600;
            padding: 0.75rem 1.5rem;
            border-radius: 12px;
            transition: all 0.3s ease;
        }

        .btn-secondary:hover {
            background: linear-gradient(135deg, #4b5563 0%, #374151 100%);
            transform: translateY(-2px);
        }

        .form-control, .form-select {
            border: 2px solid var(--border-color);
            border-radius: 12px;
            padding: 0.75rem 1rem;
            font-size: 0.95rem;
            transition: all 0.3s ease;
            background: white;
        }

        .form-control:focus, .form-select:focus {
            border-color: var(--primary-color);
            box-shadow: 0 0 0 0.2rem rgba(234, 88, 12, 0.15);
            background: white;
            transform: translateY(-1px);
        }

        .form-label {
            font-weight: 600;
            color: var(--text-dark);
            margin-bottom: 0.5rem;
            display: flex;
            align-items: center;
            gap: 0.5rem;
        }

        .form-text {
            color: var(--text-muted);
            font-size: 0.875rem;
            margin-top: 0.25rem;
        }

        .file-info {
            background: var(--bg-light);
            border-radius: 0.75rem;
            padding: 1rem;
            margin-top: 1rem;
            display: none;
        }

        .file-info.show {
            display: block;
        }

        .breadcrumb {
            background: var(--bg-light);
            border-radius: 8px;
            padding: 0.75rem 1rem;
            margin-bottom: 1rem;
        }

        .breadcrumb-item a {
            color: var(--primary-color);
            text-decoration: none;
        }

        .breadcrumb-item.active {
            color: var(--text-muted);
        }

        .upload-status {
            margin-top: 1rem;
            padding: 1rem;
            border-radius: 0.75rem;
            display: none;
        }

        .upload-status.success {
            background: rgba(16, 185, 129, 0.1);
            color: var(--success-color);
            border: 1px solid rgba(16, 185, 129, 0.2);
            display: block;
        }

        .upload-status.error {
            background: rgba(239, 68, 68, 0.1);
            color: var(--danger-color);
            border: 1px solid rgba(239, 68, 68, 0.2);
            display: block;
        }

        .file-requirements {
            background: rgba(234, 88, 12, 0.05);
            border: 1px solid rgba(234, 88, 12, 0.2);
            border-radius: 12px;
            padding: 1.25rem;
            margin-top: 1rem;
        }

        .action-buttons {
            display: flex;
            gap: 1rem;
            justify-content: flex-end;
            flex-wrap: wrap;
            margin-top: 2rem;
            padding-top: 2rem;
            border-top: 1px solid var(--border-color);
        }

        @media (max-width: 768px) {
            .main-container {
                padding: 1rem;
            }
            
            .upload-container {
                padding: 1.5rem;
            }
            
            .action-buttons {
                justify-content: stretch;
            }
            
            .action-buttons .btn {
                flex: 1;
            }
        }
    </style>
</head>
<body>
    <div class="main-container">
        <!-- Header Section -->
        <div class="header-section">
            <nav aria-label="breadcrumb">
                <ol class="breadcrumb">
                    <li class="breadcrumb-item"><a href="/dashboard">Dashboard</a></li>
                    <li class="breadcrumb-item"><a href="/videos-admin">Gestión de Videos</a></li>
                    <li class="breadcrumb-item active" aria-current="page">Subir Video</li>
                </ol>
            </nav>
            
            <div class="header-title">
                <div>
                    <h1>
                        <i class="bi bi-cloud-upload text-primary"></i>
                        Subir Nuevo Video
                    </h1>
                    <p class="header-subtitle">
                        Sube videos educativos de barbería para tus cursos
                    </p>
                </div>
            </div>
        </div>

        <!-- Upload Form -->
        <div class="upload-container">
            <form id="uploadForm" enctype="multipart/form-data">
                <!-- Información del Video -->
                <div class="form-section">
                    <h3 class="section-title">
                        <i class="bi bi-info-circle text-primary"></i>
                        Información del Video
                    </h3>
                    
                    <div class="row">
                        <div class="col-md-6 mb-3">
                            <label for="titulo" class="form-label">
                                <i class="bi bi-type"></i>
                                Título del Video *
                            </label>
                            <input type="text" class="form-control" id="titulo" name="titulo" required 
                                   placeholder="Ej: Técnicas básicas de corte degradado">
                            <div class="form-text">Título descriptivo que verán los estudiantes</div>
                        </div>
                        
                        <div class="col-md-6 mb-3">
                            <label for="duracion_minutos" class="form-label">
                                <i class="bi bi-clock"></i>
                                Duración (minutos) *
                            </label>
                            <input type="number" class="form-control" id="duracion_minutos" name="duracion_minutos" 
                                   min="1" max="180" required placeholder="45">
                            <div class="form-text">Duración aproximada del video</div>
                        </div>
                    </div>
                    
                    <div class="row">
                        <div class="col-md-6 mb-3">
                            <label for="id_modulo" class="form-label">
                                <i class="bi bi-collection"></i>
                                Módulo del Curso *
                            </label>
                            <select class="form-select" id="id_modulo" name="id_modulo" required>
                                <option value="">Selecciona un módulo...</option>
                                {{#each modulos}}
                                <option value="{{this.id_modulo}}">{{this.curso_titulo}} - {{this.titulo}}</option>
                                {{/each}}
                            </select>
                            <div class="form-text">Curso y módulo donde aparecerá el video</div>
                        </div>
                        
                        <div class="col-md-6 mb-3">
                            <label for="estatus" class="form-label">
                                <i class="bi bi-toggle-on"></i>
                                Estado del Video *
                            </label>
                            <select class="form-select" id="estatus" name="estatus" required>
                                <option value="activo">Activo (Visible para estudiantes)</option>
                                <option value="borrador">Borrador (Solo visible para instructores)</option>
                                <option value="archivado">Archivado (No visible)</option>
                            </select>
                            <div class="form-text">Define la visibilidad del video</div>
                        </div>
                    </div>

                    <div class="mb-3">
                        <label for="descripcion" class="form-label">
                            <i class="bi bi-card-text"></i>
                            Descripción
                        </label>
                        <textarea class="form-control" id="descripcion" name="descripcion" rows="3" 
                                  placeholder="Descripción detallada del contenido del video..."></textarea>
                        <div class="form-text">Descripción opcional que aparecerá bajo el video</div>
                    </div>
                </div>

                <!-- Subida de Archivo -->
                <div class="form-section">
                    <h3 class="section-title">
                        <i class="bi bi-file-earmark-play text-primary"></i>
                        Archivo de Video
                    </h3>
                    
                    <div class="file-drop-area" id="fileDropArea">
                        <input type="file" class="file-input" id="fileInput" name="video" accept="video/*" required>
                        <div class="drop-content">
                            <i class="bi bi-cloud-upload" style="font-size: 3rem; color: var(--primary-color); margin-bottom: 1rem;"></i>
                            <h4>Arrastra tu video aquí</h4>
                            <p class="text-muted">o <strong>haz clic para seleccionar</strong></p>
                            <p class="small text-muted">Formatos soportados: MP4, MOV, AVI, WMV</p>
                        </div>
                    </div>
                    
                    <div class="file-info" id="fileInfo">
                        <div class="d-flex align-items-center justify-content-between">
                            <div>
                                <strong>Archivo seleccionado:</strong>
                                <span id="fileName"></span>
                            </div>
                            <div>
                                <span class="badge bg-primary" id="fileSize"></span>
                            </div>
                        </div>
                        
                        <div class="mt-2">
                            <small class="text-muted">
                                <i class="bi bi-info-circle"></i>
                                El video se subirá a Vimeo automáticamente
                            </small>
                        </div>
                    </div>

                    <div class="file-requirements">
                        <h6><i class="bi bi-exclamation-triangle text-warning"></i> Requisitos del archivo:</h6>
                        <ul class="mb-0 small">
                            <li>Tamaño máximo: 500 MB</li>
                            <li>Formatos: MP4, MOV, AVI, WMV, WEBM</li>
                            <li>Resolución recomendada: 1080p o superior</li>
                            <li>El video se procesará automáticamente en Vimeo</li>
                        </ul>
                    </div>

                    <div class="upload-progress" id="uploadProgress">
                        <div class="d-flex justify-content-between align-items-center mb-2">
                            <span>Subiendo video...</span>
                            <span id="progressText">0%</span>
                        </div>
                        <div class="progress">
                            <div class="progress-bar" id="progressBar" role="progressbar" style="width: 0%"></div>
                        </div>
                    </div>

                    <div class="upload-status" id="uploadStatus"></div>
                </div>

                <!-- Botones de Acción -->
                <div class="action-buttons">
                    <a href="/videos-admin" class="btn btn-secondary">
                        <i class="bi bi-arrow-left me-2"></i>Cancelar
                    </a>
                    <button type="submit" class="btn btn-primary" id="submitBtn">
                        <i class="bi bi-cloud-upload me-2"></i>Subir Video a Vimeo
                    </button>
                </div>
            </form>
        </div>
    </div>

    <!-- Bootstrap 5.3 JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    
    <script>
        console.log('🎬 Iniciando sistema de subida de videos...');

        // Elementos DOM
        const fileDropArea = document.getElementById('fileDropArea');
        const fileInput = document.getElementById('fileInput');
        const fileInfo = document.getElementById('fileInfo');
        const fileName = document.getElementById('fileName');
        const fileSize = document.getElementById('fileSize');
        const uploadForm = document.getElementById('uploadForm');
        const uploadProgress = document.getElementById('uploadProgress');
        const progressBar = document.getElementById('progressBar');
        const progressText = document.getElementById('progressText');
        const uploadStatus = document.getElementById('uploadStatus');
        const submitBtn = document.getElementById('submitBtn');

        // Manejar drag & drop
        ['dragenter', 'dragover', 'dragleave', 'drop'].forEach(eventName => {
            fileDropArea.addEventListener(eventName, preventDefaults, false);
        });

        function preventDefaults(e) {
            e.preventDefault();
            e.stopPropagation();
        }

        ['dragenter', 'dragover'].forEach(eventName => {
            fileDropArea.addEventListener(eventName, highlight, false);
        });

        ['dragleave', 'drop'].forEach(eventName => {
            fileDropArea.addEventListener(eventName, unhighlight, false);
        });

        function highlight(e) {
            fileDropArea.classList.add('dragover');
        }

        function unhighlight(e) {
            fileDropArea.classList.remove('dragover');
        }

        // Manejar drop de archivos
        fileDropArea.addEventListener('drop', handleDrop, false);

        function handleDrop(e) {
            const dt = e.dataTransfer;
            const files = dt.files;
            
            if (files.length > 0) {
                fileInput.files = files;
                handleFileSelect(files[0]);
            }
        }

        // Manejar clic en área de drop
        fileDropArea.addEventListener('click', () => {
            fileInput.click();
        });

        // Manejar selección de archivo
        fileInput.addEventListener('change', (e) => {
            if (e.target.files.length > 0) {
                handleFileSelect(e.target.files[0]);
            }
        });

        function handleFileSelect(file) {
            console.log('📁 Archivo seleccionado:', file.name, file.size);
            
            // Mostrar información del archivo
            fileName.textContent = file.name;
            fileSize.textContent = formatFileSize(file.size);
            fileInfo.classList.add('show');
            fileDropArea.classList.add('has-file');
            
            // Actualizar el contenido del drop area
            fileDropArea.innerHTML = `
                <div class="drop-content">
                    <i class="bi bi-file-earmark-play" style="font-size: 3rem; color: var(--success-color); margin-bottom: 1rem;"></i>
                    <h4 class="text-success">¡Archivo seleccionado!</h4>
                    <p><strong>${file.name}</strong></p>
                    <p class="small text-muted">Haz clic para cambiar el archivo</p>
                </div>
            `;
        }

        function formatFileSize(bytes) {
            if (bytes === 0) return '0 Bytes';
            const k = 1024;
            const sizes = ['Bytes', 'KB', 'MB', 'GB'];
            const i = Math.floor(Math.log(bytes) / Math.log(k));
            return parseFloat((bytes / Math.pow(k, i)).toFixed(2)) + ' ' + sizes[i];
        }

        // Funciones de estado
        function showStatus(type, message) {
            uploadStatus.className = `upload-status ${type}`;
            uploadStatus.innerHTML = `
                <i class="bi bi-${type === 'success' ? 'check-circle' : 'exclamation-triangle'}"></i>
                ${message}
            `;
        }

        function hideStatus() {
            uploadStatus.className = 'upload-status';
        }

        // Manejar envío del formulario
        uploadForm.addEventListener('submit', async (e) => {
            e.preventDefault();

            if (!fileInput.files[0]) {
                showStatus('error', 'Por favor selecciona un archivo de video.');
                return;
            }

            const formData = new FormData();
            formData.append('video', fileInput.files[0]);
            formData.append('titulo', document.getElementById('titulo').value);
            formData.append('descripcion', document.getElementById('descripcion').value);
            formData.append('id_modulo', document.getElementById('id_modulo').value);
            formData.append('duracion_minutos', document.getElementById('duracion_minutos').value);
            formData.append('estatus', document.getElementById('estatus').value);

            // Mostrar progreso
            uploadProgress.style.display = 'block';
            submitBtn.disabled = true;
            submitBtn.innerHTML = '<i class="bi bi-hourglass-split me-2"></i>Subiendo...';
            hideStatus();

            try {
                console.log('🚀 Enviando video al servidor...');
                const response = await fetch('/videos-admin/upload', {
                    method: 'POST',
                    body: formData
                });

                const result = await response.json();
                console.log('📡 Respuesta del servidor:', result);

                if (result.success) {
                    showStatus('success', `¡Video subido exitosamente! Se ha almacenado en Vimeo.`);
                    
                    // Redirigir después de 3 segundos
                    setTimeout(() => {
                        window.location.href = '/videos-admin';
                    }, 3000);
                } else {
                    showStatus('error', 'Error al subir el video: ' + result.error);
                    submitBtn.disabled = false;
                    submitBtn.innerHTML = '<i class="bi bi-cloud-upload me-2"></i>Subir Video a Vimeo';
                }
            } catch (error) {
                console.error('❌ Error:', error);
                showStatus('error', 'Error de conexión al subir el video. Verifica tu conexión a internet.');
                submitBtn.disabled = false;
                submitBtn.innerHTML = '<i class="bi bi-cloud-upload me-2"></i>Subir Video a Vimeo';
            }

            uploadProgress.style.display = 'none';
        });

        console.log('📹 Sistema de subida de videos StartEducation iniciado');
    </script>
</body>
</html>