<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}} - StartEducation</title>
    
    <!-- Favicon -->
    <link rel="icon" type="image/svg+xml" href="/images/favicon.svg">
    
    <!-- Bootstrap 5.3 CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Bootstrap Icons -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css" rel="stylesheet">
    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    
    <style>
        :root {
            --primary-color: #ea580c;
            --primary-hover: #c2410c;
            --success-color: #10b981;
            --warning-color: #f59e0b;
            --danger-color: #ef4444;
            --text-dark: #1f2937;
            --text-muted: #6b7280;
            --border-color: #e5e7eb;
            --bg-light: #f8fafc;
        }

        * {
            font-family: 'Inter', sans-serif;
        }

        body {
            background: linear-gradient(135deg, #f8fafc 0%, #e2e8f0 100%);
            color: var(--text-dark);
            min-height: 100vh;
        }

        .main-container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 2rem;
        }

        .header-section {
            background: white;
            border-radius: 16px;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
            padding: 2rem;
            margin-bottom: 2rem;
            border-left: 5px solid var(--primary-color);
        }

        .header-title {
            display: flex;
            align-items: center;
            justify-content: space-between;
            flex-wrap: wrap;
            gap: 1rem;
        }

        .header-title h1 {
            color: var(--text-dark);
            font-weight: 700;
            margin: 0;
            display: flex;
            align-items: center;
            gap: 0.75rem;
        }

        .stats-row {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 1.5rem;
            margin-top: 1.5rem;
        }

        .stat-card {
            background: var(--bg-light);
            border-radius: 12px;
            padding: 1.5rem;
            text-align: center;
            border: 1px solid var(--border-color);
            transition: transform 0.2s ease;
        }

        .stat-card:hover {
            transform: translateY(-2px);
        }

        .stat-number {
            font-size: 2rem;
            font-weight: 700;
            color: var(--primary-color);
            display: block;
        }

        .stat-label {
            color: var(--text-muted);
            font-size: 0.9rem;
            margin-top: 0.5rem;
        }

        .certificates-section {
            background: white;
            border-radius: 16px;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
            overflow: hidden;
            margin-bottom: 2rem;
        }

        .section-header {
            background: var(--bg-light);
            padding: 1.5rem;
            border-bottom: 1px solid var(--border-color);
        }

        .certificate-card {
            padding: 1.5rem;
            border-bottom: 1px solid #f1f5f9;
            transition: background-color 0.2s ease;
        }

        .certificate-card:hover {
            background-color: rgba(234, 88, 12, 0.05);
        }

        .certificate-card:last-child {
            border-bottom: none;
        }

        .certificate-header {
            display: flex;
            justify-content: space-between;
            align-items: flex-start;
            margin-bottom: 1rem;
        }

        .certificate-info h5 {
            color: var(--text-dark);
            font-weight: 600;
            margin-bottom: 0.5rem;
        }

        .certificate-code {
            font-family: 'Courier New', monospace;
            background: #f1f5f9;
            padding: 0.25rem 0.5rem;
            border-radius: 4px;
            font-size: 0.85rem;
            color: var(--primary-color);
            font-weight: 600;
            cursor: pointer;
            transition: all 0.2s ease;
        }

        .certificate-code:hover {
            background: var(--primary-color);
            color: white;
        }

        .certificate-meta {
            display: flex;
            gap: 2rem;
            flex-wrap: wrap;
            color: var(--text-muted);
            font-size: 0.9rem;
        }

        .certificate-actions {
            display: flex;
            gap: 0.5rem;
        }

        .btn-primary {
            background: linear-gradient(135deg, var(--primary-color) 0%, var(--primary-hover) 100%);
            border: none;
            border-radius: 8px;
            font-weight: 600;
            padding: 0.5rem 1rem;
            transition: all 0.3s ease;
            color: white;
            text-decoration: none;
        }

        .btn-primary:hover {
            background: linear-gradient(135deg, var(--primary-hover) 0%, #9a3412 100%);
            transform: translateY(-2px);
            box-shadow: 0 8px 20px rgba(234, 88, 12, 0.4);
            color: white;
        }

        .btn-outline-primary {
            border: 2px solid var(--primary-color);
            color: var(--primary-color);
            background: transparent;
            border-radius: 8px;
            font-weight: 600;
            padding: 0.5rem 1rem;
            transition: all 0.3s ease;
            text-decoration: none;
        }

        .btn-outline-primary:hover {
            background: var(--primary-color);
            color: white;
        }

        .empty-state {
            text-align: center;
            padding: 4rem 2rem;
            color: var(--text-muted);
        }

        .empty-state i {
            font-size: 4rem;
            margin-bottom: 1rem;
            opacity: 0.5;
        }

        .verification-section {
            background: white;
            border-radius: 16px;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
            padding: 2rem;
            margin-bottom: 2rem;
        }

        .verification-form {
            max-width: 400px;
            margin: 0 auto;
        }

        .form-control {
            border: 2px solid var(--border-color);
            border-radius: 8px;
            padding: 0.75rem 1rem;
            font-size: 0.95rem;
            transition: border-color 0.3s ease;
        }

        .form-control:focus {
            border-color: var(--primary-color);
            box-shadow: 0 0 0 0.2rem rgba(234, 88, 12, 0.25);
        }

        .loading-overlay {
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: rgba(255, 255, 255, 0.95);
            display: none;
            align-items: center;
            justify-content: center;
            z-index: 9999;
        }

        .spinner-border {
            color: var(--primary-color);
        }

        @media (max-width: 768px) {
            .main-container {
                padding: 1rem;
            }
            
            .header-section {
                padding: 1.5rem;
            }
            
            .certificate-header {
                flex-direction: column;
                gap: 1rem;
            }
            
            .certificate-meta {
                flex-direction: column;
                gap: 0.5rem;
            }
            
            .certificate-actions {
                justify-content: stretch;
            }
        }
    </style>
</head>
<body>
    <!-- Loading Overlay -->
    <div class="loading-overlay" id="loadingOverlay">
        <div class="text-center">
            <div class="spinner-border mb-3" role="status">
                <span class="visually-hidden">Cargando...</span>
            </div>
            <p>Procesando...</p>
        </div>
    </div>

    <div class="main-container">
        <!-- Header Section -->
        <div class="header-section">
            <div class="header-title">
                <h1>
                    <i class="bi bi-award"></i>
                    Mis Certificados
                </h1>
                <div class="d-flex gap-2">
                    <a href="/user-dashboard" class="btn btn-outline-secondary">
                        <i class="bi bi-arrow-left me-2"></i>Dashboard
                    </a>
                </div>
            </div>
            
            <!-- Estadísticas del Usuario -->
            <div class="stats-row">
                <div class="stat-card">
                    <span class="stat-number">{{misCertificados.length}}</span>
                    <div class="stat-label">Mis Certificados</div>
                </div>
                <div class="stat-card">
                    <span class="stat-number">{{cursosCompletados}}</span>
                    <div class="stat-label">Cursos Completados</div>
                </div>
                <div class="stat-card">
                    <span class="stat-number">{{horasEstudio}}</span>
                    <div class="stat-label">Horas de Estudio</div>
                </div>
                <div class="stat-card">
                    <span class="stat-number">{{progreso}}%</span>
                    <div class="stat-label">Progreso General</div>
                </div>
            </div>
        </div>

        <!-- Verificación de Certificados -->
        <div class="verification-section">
            <h5 class="text-center mb-3">
                <i class="bi bi-shield-check me-2"></i>
                Verificar Certificado
            </h5>
            <div class="verification-form">
                <form id="verificarForm">
                    <div class="mb-3">
                        <input type="text" class="form-control" id="codigo_verificacion" 
                               placeholder="Ingresa el código de validación" required
                               style="font-family: 'Courier New', monospace;">
                        <div class="form-text text-center">Verifica la autenticidad de cualquier certificado</div>
                    </div>
                    <button type="submit" class="btn btn-primary w-100">
                        <i class="bi bi-search me-2"></i>Verificar Certificado
                    </button>
                </form>
                <div id="verificacion_resultado" class="mt-3" style="display: none;"></div>
            </div>
        </div>

        <!-- Mis Certificados -->
        <div class="certificates-section">
            <div class="section-header">
                <h5 class="mb-0">
                    <i class="bi bi-collection me-2"></i>
                    Mis Certificados Obtenidos
                </h5>
            </div>
            
            {{#if misCertificados.length}}
                {{#each misCertificados}}
                <div class="certificate-card">
                    <div class="certificate-header">
                        <div class="certificate-info">
                            <h5>{{curso_titulo}}</h5>
                            <div class="certificate-code" onclick="copyToClipboard('{{codigo_validacion}}')" 
                                 title="Clic para copiar código">
                                {{codigo_validacion}}
                            </div>
                        </div>
                        <div class="certificate-actions">
                            <button type="button" class="btn btn-outline-primary btn-sm" 
                                    onclick="verCertificado({{id_certificado}})">
                                <i class="bi bi-eye"></i> Ver
                            </button>
                            <button type="button" class="btn btn-primary btn-sm"
                                    onclick="descargarCertificado({{id_certificado}})">
                                <i class="bi bi-download"></i> Descargar
                            </button>
                        </div>
                    </div>
                    <div class="certificate-meta">
                        <div>
                            <i class="bi bi-person me-1"></i>
                            Instructor: {{instructor_nombre}}
                        </div>
                        <div>
                            <i class="bi bi-calendar me-1"></i>
                            Obtenido: {{formatDate fecha_emision}}
                        </div>
                        <div>
                            <i class="bi bi-check-circle me-1 text-success"></i>
                            Certificado Válido
                        </div>
                    </div>
                </div>
                {{/each}}
            {{else}}
            <div class="empty-state">
                <i class="bi bi-award"></i>
                <h4>Aún no tienes certificados</h4>
                <p>Completa tus cursos para obtener certificados de finalización.</p>
                <a href="/cursos" class="btn btn-primary">
                    <i class="bi bi-book me-2"></i>Explorar Cursos
                </a>
            </div>
            {{/if}}
        </div>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    <script src="/js/certificados-user.js"></script>
</body>
</html>