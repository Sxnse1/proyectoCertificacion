<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}} - StartEducation</title>
    
    <!-- Favicon -->
    <link rel="icon" type="image/svg+xml" href="/images/favicon.svg">
    
    <!-- Bootstrap 5.3 CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Bootstrap Icons -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css" rel="stylesheet">
    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    
    <style>
        :root {
            --primary-color: #ea580c;
            --primary-hover: #c2410c;
            --success-color: #10b981;
            --warning-color: #f59e0b;
            --danger-color: #ef4444;
            --info-color: #3b82f6;
            --text-dark: #1f2937;
            --text-muted: #6b7280;
            --border-color: #e5e7eb;
            --bg-light: #f8fafc;
        }

        * {
            font-family: 'Inter', sans-serif;
        }

        body {
            background: linear-gradient(135deg, #f8fafc 0%, #e2e8f0 100%);
            color: var(--text-dark);
            min-height: 100vh;
        }

        .main-container {
            max-width: 1400px;
            margin: 0 auto;
            padding: 2rem;
        }

        .header-section {
            background: white;
            border-radius: 16px;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
            padding: 2rem;
            margin-bottom: 2rem;
            border-left: 5px solid var(--primary-color);
        }

        .header-title {
            display: flex;
            align-items: center;
            justify-content: space-between;
            flex-wrap: wrap;
            gap: 1rem;
        }

        .header-title h1 {
            color: var(--text-dark);
            font-weight: 700;
            margin: 0;
            display: flex;
            align-items: center;
            gap: 0.75rem;
        }

        .stats-row {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 1.5rem;
            margin-top: 1.5rem;
        }

        .stat-card {
            background: var(--bg-light);
            border-radius: 12px;
            padding: 1.5rem;
            text-align: center;
            border: 1px solid var(--border-color);
            transition: transform 0.2s ease;
        }

        .stat-card:hover {
            transform: translateY(-2px);
        }

        .stat-number {
            font-size: 2rem;
            font-weight: 700;
            color: var(--primary-color);
            display: block;
        }

        .stat-label {
            color: var(--text-muted);
            font-size: 0.9rem;
            margin-top: 0.5rem;
        }

        .btn-primary {
            background: linear-gradient(135deg, var(--primary-color) 0%, var(--primary-hover) 100%);
            border: none;
            border-radius: 8px;
            font-weight: 600;
            padding: 0.75rem 1.5rem;
            transition: all 0.3s ease;
        }

        .btn-primary:hover {
            background: linear-gradient(135deg, var(--primary-hover) 0%, #9a3412 100%);
            transform: translateY(-2px);
            box-shadow: 0 8px 20px rgba(234, 88, 12, 0.4);
        }

        .btn-success {
            background: var(--success-color);
            border: none;
            border-radius: 8px;
            font-weight: 600;
            padding: 0.75rem 1.5rem;
            transition: all 0.3s ease;
        }

        .btn-success:hover {
            background: #059669;
            transform: translateY(-2px);
        }

        .btn-warning {
            background: var(--warning-color);
            border: none;
            border-radius: 8px;
            color: white;
            font-weight: 600;
            padding: 0.75rem 1.5rem;
            transition: all 0.3s ease;
        }

        .btn-warning:hover {
            background: #d97706;
            color: white;
            transform: translateY(-2px);
        }

        .btn-outline-secondary {
            border: 2px solid var(--border-color);
            color: var(--text-muted);
            border-radius: 8px;
            font-weight: 600;
            padding: 0.75rem 1.5rem;
            transition: all 0.3s ease;
        }

        .btn-outline-secondary:hover {
            background: var(--text-muted);
            border-color: var(--text-muted);
            color: white;
        }

        .plans-section {
            background: white;
            border-radius: 16px;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
            margin-bottom: 2rem;
        }

        .section-header {
            background: var(--bg-light);
            padding: 1.5rem;
            border-bottom: 1px solid var(--border-color);
        }

        .plan-card {
            background: white;
            border: 2px solid var(--border-color);
            border-radius: 16px;
            padding: 2rem;
            margin-bottom: 1.5rem;
            transition: all 0.3s ease;
            position: relative;
        }

        .plan-card:hover {
            transform: translateY(-4px);
            box-shadow: 0 12px 30px rgba(0, 0, 0, 0.15);
            border-color: var(--primary-color);
        }

        .plan-card.popular {
            border-color: var(--primary-color);
            position: relative;
        }

        .plan-card.popular::before {
            content: "Más Popular";
            position: absolute;
            top: -12px;
            left: 50%;
            transform: translateX(-50%);
            background: var(--primary-color);
            color: white;
            padding: 0.5rem 1.5rem;
            border-radius: 20px;
            font-size: 0.85rem;
            font-weight: 600;
        }

        .plan-header {
            text-align: center;
            margin-bottom: 2rem;
        }

        .plan-name {
            font-size: 1.5rem;
            font-weight: 700;
            color: var(--text-dark);
            margin-bottom: 0.5rem;
        }

        .plan-description {
            color: var(--text-muted);
            margin-bottom: 1rem;
        }

        .plan-price {
            font-size: 3rem;
            font-weight: 900;
            color: var(--primary-color);
            margin-bottom: 0.25rem;
        }

        .plan-period {
            color: var(--text-muted);
            font-size: 1rem;
        }

        .plan-features {
            list-style: none;
            padding: 0;
            margin: 2rem 0;
        }

        .plan-features li {
            display: flex;
            align-items: center;
            padding: 0.75rem 0;
            border-bottom: 1px solid #f1f5f9;
        }

        .plan-features li:last-child {
            border-bottom: none;
        }

        .plan-features li i {
            color: var(--success-color);
            margin-right: 0.75rem;
            font-size: 1.1rem;
        }

        .plan-features li.unavailable {
            opacity: 0.5;
        }

        .plan-features li.unavailable i {
            color: var(--text-muted);
        }

        .current-plan-badge {
            position: absolute;
            top: 1rem;
            right: 1rem;
            background: var(--success-color);
            color: white;
            padding: 0.5rem 1rem;
            border-radius: 6px;
            font-size: 0.85rem;
            font-weight: 600;
        }

        .subscription-status {
            background: white;
            border-radius: 16px;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
            padding: 2rem;
            margin-bottom: 2rem;
        }

        .status-card {
            background: linear-gradient(135deg, var(--success-color) 0%, #059669 100%);
            color: white;
            border-radius: 12px;
            padding: 1.5rem;
            margin-bottom: 1rem;
        }

        .status-card.inactive {
            background: linear-gradient(135deg, var(--warning-color) 0%, #d97706 100%);
        }

        .status-card.expired {
            background: linear-gradient(135deg, var(--danger-color) 0%, #dc2626 100%);
        }

        .empty-state {
            text-align: center;
            padding: 4rem 2rem;
            color: var(--text-muted);
        }

        .empty-state i {
            font-size: 4rem;
            margin-bottom: 1rem;
            opacity: 0.5;
        }

        @media (max-width: 768px) {
            .main-container {
                padding: 1rem;
            }
            
            .header-section {
                padding: 1.5rem;
            }
            
            .plan-card {
                padding: 1.5rem;
            }
            
            .plan-price {
                font-size: 2.5rem;
            }
        }
    </style>
</head>
<body>

    <div class="main-container">
        <!-- Header Section -->
        <div class="header-section">
            <div class="header-title">
                <h1>
                    <i class="bi bi-star"></i>
                    Planes de Suscripción
                </h1>
                <div class="d-flex gap-2">
                    <a href="/user-dashboard" class="btn btn-outline-secondary">
                        <i class="bi bi-arrow-left me-2"></i>Volver al Dashboard
                    </a>
                </div>
            </div>
            
            <!-- Estadísticas -->
            <div class="stats-row">
                <div class="stat-card">
                    <span class="stat-number">{{stats.totalPlanes}}</span>
                    <div class="stat-label">Planes Disponibles</div>
                </div>
                <div class="stat-card">
                    <span class="stat-number">{{stats.usuariosActivos}}</span>
                    <div class="stat-label">Usuarios Activos</div>
                </div>
                <div class="stat-card">
                    <span class="stat-number">{{stats.descuentoActual}}%</span>
                    <div class="stat-label">Descuento Especial</div>
                </div>
                <div class="stat-card">
                    <span class="stat-number">{{#if currentSubscription.active}}Activa{{else}}Inactiva{{/if}}</span>
                    <div class="stat-label">Mi Suscripción</div>
                </div>
            </div>
        </div>

        <!-- Current Subscription Status -->
        {{#if currentSubscription}}
        <div class="subscription-status">
            <div class="d-flex justify-content-between align-items-center mb-3">
                <h5 class="mb-0">
                    <i class="bi bi-credit-card me-2"></i>
                    Estado Actual de Suscripción
                </h5>
            </div>
            
            <div class="status-card {{#unless currentSubscription.active}}inactive{{/unless}}">
                <div class="d-flex justify-content-between align-items-start">
                    <div>
                        <h6 class="mb-1">
                            {{#if currentSubscription.active}}
                            <i class="bi bi-check-circle me-2"></i>
                            Suscripción Activa - {{currentSubscription.planName}}
                            {{else}}
                            <i class="bi bi-exclamation-triangle me-2"></i>
                            Sin Suscripción Activa
                            {{/if}}
                        </h6>
                        <p class="mb-2">
                            {{#if currentSubscription.active}}
                            Acceso completo a todos los cursos y contenido premium
                            {{else}}
                            Accede a contenido premium con una suscripción
                            {{/if}}
                        </p>
                        <small>
                            {{#if currentSubscription.active}}
                            <i class="bi bi-calendar-event me-1"></i>
                            Renovación: {{formatDate currentSubscription.endDate}}
                            {{else}}
                            <i class="bi bi-info-circle me-1"></i>
                            Explora nuestros planes disponibles
                            {{/if}}
                        </small>
                    </div>
                    <div class="text-end">
                        {{#if currentSubscription.active}}
                        <div class="fw-bold">${{currentSubscription.price}}/mes</div>
                        <small>Próximo pago</small>
                        {{else}}
                        <div class="fw-bold">$0.00</div>
                        <small>Sin costo actual</small>
                        {{/if}}
                    </div>
                </div>
            </div>
        </div>
        {{/if}}

        <!-- Plans Section -->
        <div class="plans-section">
            <div class="section-header">
                <h5 class="mb-0">
                    <i class="bi bi-grid-3x3-gap me-2"></i>
                    Planes de Membresía Disponibles
                </h5>
            </div>
            <div class="p-4">
                {{#if membresias}}
                <div class="row">
                    {{#each membresias}}
                    <div class="col-lg-4 col-md-6 mb-4">
                        <div class="plan-card {{#if (eq @index 1)}}popular{{/if}}">
                            {{#if ../currentSubscription}}
                            {{#if (eq ../currentSubscription.membershipId this.id_membresia)}}
                            <div class="current-plan-badge">Plan Actual</div>
                            {{/if}}
                            {{/if}}
                            
                            <div class="plan-header">
                                <div class="plan-name">{{nombre}}</div>
                                <div class="plan-description">{{descripcion}}</div>
                                <div class="plan-price">${{precio}}</div>
                                <div class="plan-period">
                                    {{#if (eq tipo_periodo 'mensual')}}
                                    por mes
                                    {{else if (eq tipo_periodo 'anual')}}
                                    por año
                                    {{else}}
                                    pago único
                                    {{/if}}
                                </div>
                            </div>

                            <ul class="plan-features">
                                {{#if beneficios}}
                                {{#each (split beneficios '|')}}
                                <li>
                                    <i class="bi bi-check-circle"></i>
                                    {{this}}
                                </li>
                                {{/each}}
                                {{else}}
                                <li>
                                    <i class="bi bi-check-circle"></i>
                                    Acceso a cursos incluidos
                                </li>
                                <li>
                                    <i class="bi bi-check-circle"></i>
                                    Soporte por email
                                </li>
                                <li>
                                    <i class="bi bi-check-circle"></i>
                                    Certificados de finalización
                                </li>
                                {{#if (gt @index 0)}}
                                <li>
                                    <i class="bi bi-check-circle"></i>
                                    Soporte prioritario
                                </li>
                                <li>
                                    <i class="bi bi-check-circle"></i>
                                    Acceso a contenido premium
                                </li>
                                {{/if}}
                                {{#if (gt @index 1)}}
                                <li>
                                    <i class="bi bi-check-circle"></i>
                                    Mentoring 1:1
                                </li>
                                <li>
                                    <i class="bi bi-check-circle"></i>
                                    Proyectos personalizados
                                </li>
                                {{/if}}
                                {{/if}}
                            </ul>

                            <div class="text-center">
                                {{#if ../currentSubscription}}
                                {{#if (eq ../currentSubscription.membershipId this.id_membresia)}}
                                <button class="btn btn-success" disabled>
                                    <i class="bi bi-check-circle me-2"></i>Plan Actual
                                </button>
                                {{else}}
                                <button class="btn btn-primary subscribe-btn" data-membership-id="{{id_membresia}}" data-plan-name="{{nombre}}" data-price="{{precio}}">
                                    <i class="bi bi-arrow-up-circle me-2"></i>Cambiar Plan
                                </button>
                                {{/if}}
                                {{else}}
                                <button class="btn btn-primary subscribe-btn" data-membership-id="{{id_membresia}}" data-plan-name="{{nombre}}" data-price="{{precio}}">
                                    <i class="bi bi-cart-plus me-2"></i>Suscribirse Ahora
                                </button>
                                {{/if}}
                            </div>
                        </div>
                    </div>
                    {{/each}}
                </div>

                <div class="text-center mt-4">
                    <div class="alert alert-info">
                        <i class="bi bi-info-circle me-2"></i>
                        <strong>¿Tienes dudas?</strong> Contacta nuestro equipo de soporte para ayudarte a elegir el plan perfecto.
                        <a href="/contacto" class="alert-link">Contactar Soporte</a>
                    </div>
                </div>
                {{else}}
                <div class="empty-state">
                    <i class="bi bi-star"></i>
                    <h4>No hay planes disponibles</h4>
                    <p>Los planes de suscripción estarán disponibles próximamente.</p>
                </div>
                {{/if}}
            </div>
        </div>
    </div>

    <!-- Subscription Modal -->
    <div class="modal fade" id="subscriptionModal" tabindex="-1" aria-labelledby="subscriptionModalLabel" aria-hidden="true">
        <div class="modal-dialog">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" id="subscriptionModalLabel">
                        <i class="bi bi-star me-2"></i>Confirmar Suscripción
                    </h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <div class="modal-body">
                    <div class="text-center mb-4">
                        <i class="bi bi-credit-card" style="font-size: 3rem; color: var(--primary-color);"></i>
                        <h4 class="mt-3">¿Confirmar suscripción?</h4>
                        <p class="text-muted">Vas a suscribirte al plan <strong id="planName"></strong></p>
                    </div>
                    
                    <div class="alert alert-info">
                        <div class="d-flex justify-content-between">
                            <span>Plan:</span>
                            <strong id="modalPlanName"></strong>
                        </div>
                        <div class="d-flex justify-content-between">
                            <span>Precio:</span>
                            <strong id="modalPrice"></strong>
                        </div>
                        <div class="d-flex justify-content-between">
                            <span>Facturación:</span>
                            <strong>Mensual</strong>
                        </div>
                    </div>
                    
                    <div class="alert alert-warning">
                        <i class="bi bi-exclamation-triangle me-2"></i>
                        <small>Esta es una demostración. En un entorno real, aquí se integraría con un procesador de pagos como Stripe o PayPal.</small>
                    </div>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-outline-secondary" data-bs-dismiss="modal">Cancelar</button>
                    <button type="button" class="btn btn-primary" id="confirmSubscription">
                        <i class="bi bi-check-circle me-2"></i>Confirmar Suscripción
                    </button>
                </div>
            </div>
        </div>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    
    <script>
        // Subscription functionality
        document.addEventListener('DOMContentLoaded', function() {
            const subscribeButtons = document.querySelectorAll('.subscribe-btn');
            const subscriptionModal = new bootstrap.Modal(document.getElementById('subscriptionModal'));
            const confirmButton = document.getElementById('confirmSubscription');
            
            let selectedMembershipId = null;
            let selectedPlanName = '';
            let selectedPrice = '';

            // Handle subscribe button clicks
            subscribeButtons.forEach(button => {
                button.addEventListener('click', function() {
                    const membershipId = this.dataset.membershipId;
                    const planName = this.dataset.planName;
                    const price = this.dataset.price;
                    
                    selectedMembershipId = membershipId;
                    selectedPlanName = planName;
                    selectedPrice = price;
                    
                    // Update modal content
                    document.getElementById('planName').textContent = planName;
                    document.getElementById('modalPlanName').textContent = planName;
                    document.getElementById('modalPrice').textContent = `$${price}/mes`;
                    
                    subscriptionModal.show();
                });
            });

            // Handle subscription confirmation
            confirmButton.addEventListener('click', function() {
                if (!selectedMembershipId) return;
                
                // Show loading state
                this.innerHTML = '<i class="bi bi-hourglass-split me-2"></i>Procesando...';
                this.disabled = true;
                
                // Simulate API call
                fetch('/suscripciones/subscribe', {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json',
                    },
                    body: JSON.stringify({
                        membershipId: selectedMembershipId,
                        planName: selectedPlanName,
                        price: selectedPrice
                    })
                })
                .then(response => response.json())
                .then(data => {
                    if (data.success) {
                        // Show success message
                        subscriptionModal.hide();
                        
                        // Show success alert
                        const alertDiv = document.createElement('div');
                        alertDiv.className = 'alert alert-success alert-dismissible fade show';
                        alertDiv.innerHTML = `
                            <i class="bi bi-check-circle me-2"></i>
                            <strong>¡Suscripción exitosa!</strong> ${data.message}
                            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                        `;
                        
                        document.querySelector('.main-container').insertBefore(alertDiv, document.querySelector('.header-section'));
                        
                        // Redirect after 3 seconds
                        setTimeout(() => {
                            if (data.redirectUrl) {
                                window.location.href = data.redirectUrl;
                            } else {
                                window.location.reload();
                            }
                        }, 3000);
                    } else {
                        throw new Error(data.message || 'Error en la suscripción');
                    }
                })
                .catch(error => {
                    console.error('Error:', error);
                    
                    // Show error alert
                    const alertDiv = document.createElement('div');
                    alertDiv.className = 'alert alert-danger alert-dismissible fade show';
                    alertDiv.innerHTML = `
                        <i class="bi bi-exclamation-triangle me-2"></i>
                        <strong>Error:</strong> ${error.message}
                        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                    `;
                    
                    document.querySelector('.modal-body').insertBefore(alertDiv, document.querySelector('.modal-body').firstChild);
                })
                .finally(() => {
                    // Reset button state
                    confirmButton.innerHTML = '<i class="bi bi-check-circle me-2"></i>Confirmar Suscripción';
                    confirmButton.disabled = false;
                });
            });

            // Animate cards on load
            const planCards = document.querySelectorAll('.plan-card');
            planCards.forEach((card, index) => {
                card.style.opacity = '0';
                card.style.transform = 'translateY(20px)';
                setTimeout(() => {
                    card.style.transition = 'all 0.6s ease';
                    card.style.opacity = '1';
                    card.style.transform = 'translateY(0)';
                }, index * 150);
            });
        });
    </script>
</body>
</html>