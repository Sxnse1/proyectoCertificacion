<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}}</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container mt-4">
        <h1>Logs de Auditoría</h1>
        
        <div class="row">
            <div class="col-12">
                <h3>Información de Debug</h3>
                <ul>
                    <li>Logs encontrados: {{logs.length}}</li>
                    <li>Stats disponibles: {{#if stats}}Sí{{else}}No{{/if}}</li>
                    <li>Usuario actual: {{currentUser.nombre}}</li>
                    <li>Es admin: {{isAdmin}}</li>
                </ul>
            </div>
        </div>

        {{#if logs}}
        <div class="row mt-4">
            <div class="col-12">
                <h3>Logs</h3>
                <div class="table-responsive">
                    <table class="table table-striped">
                        <thead>
                            <tr>
                                <th>ID</th>
                                <th>Usuario</th>
                                <th>Acción</th>
                                <th>Entidad</th>
                                <th>Timestamp</th>
                            </tr>
                        </thead>
                        <tbody>
                            {{#each logs}}
                            <tr>
                                <td>{{LogID}}</td>
                                <td>{{UsuarioNombre}} ({{UsuarioEmail}})</td>
                                <td>{{Accion}}</td>
                                <td>{{Entidad}}</td>
                                <td>{{Timestamp}}</td>
                            </tr>
                            {{/each}}
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
        {{else}}
        <div class="alert alert-info">
            No hay logs de auditoría disponibles.
        </div>
        {{/if}}
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>