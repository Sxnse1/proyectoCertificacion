<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}} - StartEducation</title>
    
    <!-- Favicon -->
    <link rel="icon" type="image/svg+xml" href="/images/favicon.svg">
    
    <!-- Bootstrap 5.3 CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Bootstrap Icons -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css" rel="stylesheet">
    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    
    <style>
        :root {
            --primary-color: #ea580c;
            --primary-hover: #c2410c;
            --success-color: #10b981;
            --warning-color: #f59e0b;
            --danger-color: #ef4444;
            --text-dark: #1f2937;
            --text-muted: #6b7280;
            --border-color: #e5e7eb;
            --bg-light: #f8fafc;
        }

        * {
            font-family: 'Inter', sans-serif;
        }

        body {
            background: linear-gradient(135deg, #f8fafc 0%, #e2e8f0 100%);
            color: var(--text-dark);
            min-height: 100vh;
        }

        .main-container {
            max-width: 1400px;
            margin: 0 auto;
            padding: 2rem;
        }

        .header-section {
            background: white;
            border-radius: 16px;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
            padding: 2rem;
            margin-bottom: 2rem;
            border-left: 5px solid var(--primary-color);
        }

        .header-title {
            display: flex;
            align-items: center;
            justify-content: space-between;
            flex-wrap: wrap;
            gap: 1rem;
        }

        .header-title h1 {
            color: var(--text-dark);
            font-weight: 700;
            margin: 0;
            display: flex;
            align-items: center;
            gap: 0.75rem;
        }

        .stats-row {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 1.5rem;
            margin-top: 1.5rem;
        }

        .stat-card {
            background: var(--bg-light);
            border-radius: 12px;
            padding: 1.5rem;
            text-align: center;
            border: 1px solid var(--border-color);
            transition: transform 0.2s ease;
        }

        .stat-card:hover {
            transform: translateY(-2px);
        }

        .stat-number {
            font-size: 2rem;
            font-weight: 700;
            color: var(--primary-color);
            display: block;
        }

        .stat-label {
            color: var(--text-muted);
            font-size: 0.9rem;
            margin-top: 0.5rem;
        }

        .controls-section {
            background: white;
            border-radius: 16px;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
            padding: 1.5rem;
            margin-bottom: 2rem;
        }

        .controls-row {
            display: flex;
            justify-content: space-between;
            align-items: center;
            flex-wrap: wrap;
            gap: 1rem;
        }

        .search-group {
            display: flex;
            gap: 0.5rem;
            flex: 1;
            max-width: 400px;
        }

        .search-input {
            border: 2px solid var(--border-color);
            border-radius: 8px;
            padding: 0.75rem 1rem;
            font-size: 0.95rem;
            transition: border-color 0.3s ease;
        }

        .search-input:focus {
            border-color: var(--primary-color);
            box-shadow: 0 0 0 0.2rem rgba(234, 88, 12, 0.25);
        }

        .btn-primary {
            background: linear-gradient(135deg, var(--primary-color) 0%, var(--primary-hover) 100%);
            border: none;
            border-radius: 8px;
            font-weight: 600;
            padding: 0.75rem 1.5rem;
            transition: all 0.3s ease;
        }

        .btn-primary:hover {
            background: linear-gradient(135deg, var(--primary-hover) 0%, #9a3412 100%);
            transform: translateY(-2px);
            box-shadow: 0 8px 20px rgba(234, 88, 12, 0.4);
        }

        .btn-success {
            background: var(--success-color);
            border: none;
            border-radius: 8px;
            font-weight: 600;
            padding: 0.75rem 1.5rem;
            transition: all 0.3s ease;
        }

        .btn-success:hover {
            background: #059669;
            transform: translateY(-2px);
        }

        .btn-warning {
            background: var(--warning-color);
            border: none;
            border-radius: 8px;
            color: white;
            font-weight: 600;
            padding: 0.5rem 1rem;
            transition: all 0.3s ease;
        }

        .btn-warning:hover {
            background: #d97706;
            color: white;
        }

        .btn-danger {
            background: var(--danger-color);
            border: none;
            border-radius: 8px;
            font-weight: 600;
            padding: 0.5rem 1rem;
            transition: all 0.3s ease;
        }

        .btn-danger:hover {
            background: #dc2626;
        }

        .table-section {
            background: white;
            border-radius: 16px;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
            overflow: hidden;
        }

        .table-header {
            background: var(--bg-light);
            padding: 1.5rem;
            border-bottom: 1px solid var(--border-color);
        }

        .table {
            margin: 0;
        }

        .table th {
            background: var(--bg-light);
            border: none;
            font-weight: 600;
            color: var(--text-dark);
            padding: 1rem;
        }

        .table td {
            border: none;
            padding: 1rem;
            vertical-align: middle;
        }

        .table tbody tr {
            border-bottom: 1px solid #f1f5f9;
            transition: background-color 0.2s ease;
        }

        .table tbody tr:hover {
            background-color: rgba(234, 88, 12, 0.05);
        }

        .badge {
            font-size: 0.75rem;
            padding: 0.35rem 0.65rem;
        }

        .actions-group {
            display: flex;
            gap: 0.5rem;
        }

        .action-buttons {
            display: flex;
            gap: 0.5rem;
        }

        .pagination-section {
            background: white;
            border-radius: 16px;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
            padding: 1.5rem;
            margin-top: 2rem;
        }

        .pagination {
            margin: 0;
            justify-content: center;
        }

        .page-link {
            border: none;
            color: var(--text-muted);
            padding: 0.75rem 1rem;
            margin: 0 0.25rem;
            border-radius: 8px;
            transition: all 0.3s ease;
        }

        .page-link:hover {
            background: var(--primary-color);
            color: white;
        }

        .page-item.active .page-link {
            background: var(--primary-color);
            color: white;
        }

        .empty-state {
            text-align: center;
            padding: 4rem 2rem;
            color: var(--text-muted);
        }

        .empty-state i {
            font-size: 4rem;
            margin-bottom: 1rem;
            opacity: 0.5;
        }

        .loading-overlay {
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: rgba(255, 255, 255, 0.95);
            display: none;
            align-items: center;
            justify-content: center;
            z-index: 9999;
        }

        .spinner-border {
            color: var(--primary-color);
        }

        .curso-item {
            display: flex;
            align-items: center;
            gap: 0.75rem;
        }

        .curso-image {
            width: 40px;
            height: 40px;
            border-radius: 8px;
            object-fit: cover;
        }

        .curso-info h6 {
            margin: 0;
            font-size: 0.9rem;
            font-weight: 600;
        }

        .curso-info small {
            color: var(--text-muted);
        }

        .tasa-conversion {
            font-weight: 600;
        }

        .tasa-alta {
            color: var(--success-color);
        }

        .tasa-media {
            color: var(--warning-color);
        }

        .tasa-baja {
            color: var(--danger-color);
        }

        .trending-badge {
            background: linear-gradient(135deg, #ff6b6b, #feca57);
            color: white;
            animation: pulse 1.5s infinite;
        }

        @keyframes pulse {
            0% { opacity: 1; }
            50% { opacity: 0.7; }
            100% { opacity: 1; }
        }

        @media (max-width: 768px) {
            .main-container {
                padding: 1rem;
            }
            
            .header-section {
                padding: 1.5rem;
            }
            
            .controls-row {
                flex-direction: column;
                align-items: stretch;
            }
            
            .search-group {
                max-width: none;
            }
            
            .actions-group {
                justify-content: center;
            }
            
            .table-responsive {
                font-size: 0.9rem;
            }
        }
    </style>
</head>
<body>
    <!-- Loading Overlay -->
    <div class="loading-overlay" id="loadingOverlay">
        <div class="text-center">
            <div class="spinner-border mb-3" role="status">
                <span class="visually-hidden">Cargando...</span>
            </div>
            <p>Procesando...</p>
        </div>
    </div>

    <div class="main-container">
        <!-- Header Section -->
        <div class="header-section">
            <div class="header-title">
                <h1>
                    <i class="bi bi-heart"></i>
                    Gestión de Favoritos
                </h1>
                <div class="d-flex gap-2">
                    <a href="/dashboard" class="btn btn-outline-secondary">
                        <i class="bi bi-arrow-left me-2"></i>Dashboard
                    </a>
                </div>
            </div>
            
            <!-- Estadísticas -->
            <div class="stats-row">
                <div class="stat-card">
                    <span class="stat-number">{{stats.totalFavoritos}}</span>
                    <div class="stat-label">Total Favoritos</div>
                </div>
                <div class="stat-card">
                    <span class="stat-number">{{stats.usuariosActivos}}</span>
                    <div class="stat-label">Usuarios Activos</div>
                </div>
                <div class="stat-card">
                    <span class="stat-number">{{stats.promedioFavoritos}}</span>
                    <div class="stat-label">Promedio por Usuario</div>
                </div>
            </div>
        </div>

        <!-- Controls Section -->
        <div class="controls-section">
            <div class="controls-row">
                <div class="search-group">
                    <form method="GET" class="d-flex w-100">
                        <input type="text" 
                               class="form-control search-input" 
                               name="search" 
                               placeholder="Buscar cursos favoritos..." 
                               value="{{search}}">
                        <button type="submit" class="btn btn-primary ms-2">
                            <i class="bi bi-search"></i>
                        </button>
                    </form>
                </div>
                <div class="action-buttons">
                    <button type="button" class="btn btn-success" onclick="exportarFavoritos()">
                        <i class="bi bi-download me-2"></i>Exportar
                    </button>
                </div>
            </div>
        </div>

        <!-- Top Usuarios -->
        {{#if topUsuarios.length}}
        <div class="table-section mb-4">
            <div class="table-header">
                <h5 class="mb-0">
                    <i class="bi bi-people me-2"></i>
                    Top 10 Usuarios con Más Favoritos
                </h5>
            </div>
            <div class="table-responsive">
                <table class="table">
                    <thead>
                        <tr>
                            <th>Usuario</th>
                            <th>Email</th>
                            <th>Favoritos</th>
                            <th>Cursos Comprados</th>
                            <th>Último Favorito</th>
                        </tr>
                    </thead>
                    <tbody>
                        {{#each topUsuarios}}
                        <tr>
                            <td><strong>{{this.nombre}}</strong></td>
                            <td>{{this.email}}</td>
                            <td><span class="badge bg-primary">{{this.cantidad_favoritos}}</span></td>
                            <td><span class="badge bg-success">{{this.cursos_comprados}}</span></td>
                            <td>{{formatDate this.ultimo_favorito}}</td>
                        </tr>
                        {{/each}}
                    </tbody>
                </table>
            </div>
        </div>
        {{/if}}

        <!-- Table Section -->
        <div class="table-section">
            <div class="table-header">
                <h5 class="mb-0">
                    <i class="bi bi-list-ul me-2"></i>
                    Cursos Más Guardados en Favoritos
                    {{#if search}}
                        <small class="text-muted">- Resultados para "{{search}}"</small>
                    {{/if}}
                </h5>
            </div>
            
            {{#if cursosFavoritos.length}}
            <div class="table-responsive">
                <table class="table" id="favoritosTable">
                    <thead>
                        <tr>
                            <th>Curso</th>
                            <th>Instructor</th>
                            <th>Favoritos</th>
                            <th>Compras</th>
                            <th>Tasa Conversión</th>
                            <th>Estado</th>
                        </tr>
                    </thead>
                    <tbody>
                        {{#each cursosFavoritos}}
                        <tr>
                            <td>
                                <div class="curso-item">
                                    {{#if this.miniatura}}
                                    <img src="{{this.miniatura}}" alt="{{this.titulo}}" class="curso-image">
                                    {{else}}
                                    <div class="curso-image bg-light d-flex align-items-center justify-content-center">
                                        <i class="bi bi-book"></i>
                                    </div>
                                    {{/if}}
                                    <div class="curso-info">
                                        <h6>{{this.titulo}}</h6>
                                        <small>ID: {{this.id_curso}}</small>
                                    </div>
                                </div>
                            </td>
                            <td>{{this.instructor_nombre}}</td>
                            <td>
                                <span class="badge bg-info">
                                    <i class="bi bi-heart"></i> {{this.cantidad_favoritos}}
                                </span>
                            </td>
                            <td>
                                <span class="badge bg-success">
                                    <i class="bi bi-cart-check"></i> {{this.cantidad_compras}}
                                </span>
                            </td>
                            <td>
                                <span class="tasa-conversion 
                                    {{#if (gte this.tasa_conversion 30)}}tasa-alta
                                    {{else if (gte this.tasa_conversion 15)}}tasa-media
                                    {{else}}tasa-baja{{/if}}">
                                    {{this.tasa_conversion}}%
                                </span>
                            </td>
                            <td>
                                {{#if this.trending}}
                                <span class="badge trending-badge">
                                    <i class="bi bi-fire"></i> Trending
                                </span>
                                {{else}}
                                <span class="badge bg-secondary">Normal</span>
                                {{/if}}
                            </td>
                        </tr>
                        {{/each}}
                    </tbody>
                </table>
            </div>
            {{else}}
            <div class="empty-state">
                <i class="bi bi-heart"></i>
                <h4>No hay favoritos</h4>
                <p>Cuando los usuarios guarden cursos como favoritos, aparecerán aquí.</p>
            </div>
            {{/if}}
        </div>

        <!-- Estadísticas por Categoría -->
        {{#if estadisticasCategorias.length}}
        <div class="table-section mt-4">
            <div class="table-header">
                <h5 class="mb-0">
                    <i class="bi bi-pie-chart me-2"></i>
                    Favoritos por Categoría
                </h5>
            </div>
            <div class="table-responsive">
                <table class="table">
                    <thead>
                        <tr>
                            <th>Categoría</th>
                            <th>Cantidad</th>
                            <th>Porcentaje</th>
                        </tr>
                    </thead>
                    <tbody>
                        {{#each estadisticasCategorias}}
                        <tr>
                            <td><strong>{{this.nombre}}</strong></td>
                            <td><span class="badge bg-primary">{{this.cantidad}}</span></td>
                            <td>
                                <div class="d-flex align-items-center">
                                    <div class="progress flex-grow-1 me-2" style="height: 8px;">
                                        <div class="progress-bar bg-primary" style="width: {{this.porcentaje}}%"></div>
                                    </div>
                                    <small>{{this.porcentaje}}%</small>
                                </div>
                            </td>
                        </tr>
                        {{/each}}
                    </tbody>
                </table>
            </div>
        </div>
        {{/if}}
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    
    <script>
        // Inicializar cuando carga la página
        document.addEventListener('DOMContentLoaded', function() {
            // Configuraciones iniciales si las hay
        });
        
        // Exportar datos de favoritos
        function exportarFavoritos() {
            showLoading(true);
            
            // Crear un enlace temporal para descargar
            const link = document.createElement('a');
            link.href = '/admin/favoritos/export';
            link.download = 'favoritos_' + new Date().toISOString().split('T')[0] + '.csv';
            document.body.appendChild(link);
            link.click();
            document.body.removeChild(link);
            
            showLoading(false);
        }
        
        // Mostrar/ocultar loading
        function showLoading(show) {
            const overlay = document.getElementById('loadingOverlay');
            overlay.style.display = show ? 'flex' : 'none';
        }
    </script>
</body>
</html>